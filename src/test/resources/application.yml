spring:
 application:
   name: lush-app-test

logging:
  level:
    root: INFO
    com.px3j.lush: DEBUG

lush:
  security:
    protected-paths: /lush/example/**
#    public-paths: /actuator/**, /health/**
